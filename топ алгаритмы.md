1) Удаление лейбла:
  - если это не 'All notes' или 'Trashed notes', то можно удалять, иначе соси хуй
  - при удалении лейбла, нужно удалить все заметки с этим лейблом, и прыгнуть на 'All notes'
   - dispatch(deleteLabel(label))
   - dispatch(deleteNotesWithLabel(label)) // положить в корзину
   - navigate('/')
2) Изменение лейбла:
  всё то же самое, что и выше, только:
   - поменять всем заметкам лейбл
   - не делать navigate()
  
БЛЯЯЯ, пчелллл, нахуй хранить дубликат лейбла в заметке, если можно хранить ссылку на элемент из labels????